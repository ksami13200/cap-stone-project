apiVersion: v1
kind: LimitRange
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  limits:
    - type: {{ .Values.frontend.limits.type }}
      default:
        cpu: {{ .Values.frontend.limits.default.cpu }}
        memory: {{ .Values.frontend.limits.default.memory }}
      defaultRequest:
        cpu: {{ .Values.frontend.limits.defaultRequest.cpu }}
        memory: {{ .Values.frontend.limits.defaultRequest.memory }}
