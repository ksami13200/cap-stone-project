apiVersion: v1
kind: LimitRange
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.mysql.namespace }}
spec:
  limits:
    - type: {{ .Values.mysql.limits.type }}
      default:
        cpu: {{ .Values.mysql.limits.default.cpu }}
        memory: {{ .Values.mysql.limits.default.memory }}
      defaultRequest:
        cpu: {{ .Values.mysql.limits.defaultRequest.cpu }}
        memory: {{ .Values.mysql.limits.defaultRequest.memory }}
